(async function(){const db=window.shelfTalkDB,grid=document.getElementById('eventsGrid');if(!grid)return;if(!db){grid.innerHTML='<div class="empty-state"><h3>Events calendar ready.</h3><p>Connect Supabase to load live events.</p></div>';return;}const {data,error}=await db.from('events').select('*').eq('status','published').gte('starts_at',new Date().toISOString()).order('starts_at',{ascending:true});if(error||!data?.length){grid.innerHTML='<div class="empty-state"><h3>No upcoming events.</h3><p>Check back soon for new ShelfTalk gatherings.</p></div>';return;}grid.innerHTML=data.map(e=>`<article class="event-card"><div class="event-image" style="background-image:url('${e.image_url||'https://images.unsplash.com/photo-1507842217343-583bb7270b66?auto=format&fit=crop&w=1200&q=85'}')"></div><div class="event-card-body"><span class="eyebrow">${new Date(e.starts_at).toLocaleString(undefined,{dateStyle:'medium',timeStyle:'short'})}</span><h3>${e.title}</h3><p>${e.description||''}</p><p class="event-meta">${e.location||'Online ShelfTalk gathering'}</p>${e.meeting_url?`<a class="btn btn-primary" target="_blank" rel="noopener" href="${e.meeting_url}">Join event</a>`:'<span class="status-pill">Details coming soon</span>'}</div></article>`).join('');})();
